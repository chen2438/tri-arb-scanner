# 发布验收摘要

> 验收日期：2026-07-21
>
> 本文件只记录可复现的发布证据，不改变 `DOCS.md` 中的功能、架构或安全规范。

## 结论

MEXC v0.1 长跑和 MEXC + OKX 多交易所增量验收均通过。扫描器在没有 API Key 的情况下完成公共现货
行情、深度确认、受控恢复、SQLite 审计和网页验证。验收期间观察到真实的 20 档深度确认机会；这些
结果均为预估，三腿不能原子成交，不代表或保证实际利润。

## MEXC + OKX 多交易所验收

- 同一应用进程同时达到 `ready`，总计 3,347 个市场、4,880 条有向三角路径；
- MEXC：2,039 个市场、1,612 条路径、42 个深度订阅/深度簿，无最近错误；
- OKX：1,308 个市场、3,268 条路径、60 个深度订阅/深度簿、60 份动态价格限制，无最近错误；
- 聚合扫描每轮保留两所各自 Top 20，共 40 条候选；抽样轮有 16 条通过当前代次三腿深度确认；
- OKX 单市场真实 `books` 流连续收到 3 个版本，三次均重建出 20 bids + 20 asks；
- OKX 独立服务验收达到 30 个核心市场、覆盖 60 条路径并收到 30 本实时深度簿；
- 浏览器真实页面显示 MEXC/OKX 分所状态、全部连接、交易所筛选和扫描诊断，控制台无错误；
- 活跃机会、历史、近似机会与审计快照均携带交易所标识；跨交易所三腿在领域模型层被拒绝。

## 真实行情长跑

- 连续观测窗口：2026-07-21 13:47:25–14:17:37 UTC，共 30 分 12 秒；
- 中点执行一次干净停止并在 6 秒内使用同一 SQLite 重启，用于验证恢复路径；
- 市场规模：启动时 2,040 个市场，结束时 2,039 个市场；全程 736 条 USDT 三角路径；
- 元数据按 5 分钟周期成功热刷新 3 次，服务器时钟与全市场报价持续按各自周期刷新；
- 最终报价年龄 49 ms，最近错误为空；未观察到 REST 429、5xx、WebSocket backoff 或协议拒绝；
- MEXC 深度分片、Top 20 候选和动态订阅持续工作，修复后全局订阅数始终与分片明细合计一致；
- 受控重启时活跃生命周期以 `process_restart` 关闭，重启后使用全新行情和 UUID 建立新生命周期；
- 已打开网页在服务重启后自动恢复 `ready / 实时连接`，等待新 snapshot 期间不沿用本地增量；最终
  无 degraded 横幅、无横向溢出，浏览器控制台无错误或警告；
- 观察到已确认机会，也观察到缺深度或陈旧深度导致的立即关闭；没有为了验收生成演示机会。

## SQLite 审计结果

干净停机后的独立查询结果：

| 检查 | 结果 |
| --- | ---: |
| schema version | 1 |
| journal mode | WAL |
| integrity check | ok |
| 生命周期 | 14 |
| 残留活跃生命周期 | 0 |
| 审计事件 | 81 |
| opened / peak / closed | 14 / 53 / 14 |

API 活跃状态与 SQLite 写入在生命周期边界上可能因毫秒级先后出现瞬时采样差异；同秒复查一致，干净
停机后不存在丢失的关闭事件或残留活跃记录。

## 自动化与诊断

- Ruff、固定 Protobuf 生成漂移检查通过；
- Pytest：117 项通过；
- Vitest：7 项通过；TypeScript 与 Vite 生产构建通过；
- `tri-arb doctor` 的配置、SQLite 临时写、Protobuf、MEXC ping/time/exchangeInfo/bookTicker 均通过；
- 3,000 市场、2,000 路径的广筛夹具本机 5 次约 4.3–5.6 ms，CI 上限为 250 ms；
- 桌面端与 390 px 移动端真实 MEXC 行情浏览器检查通过，无横向溢出或控制台错误。

多交易所功能合入后的最新复验：

- Ruff 通过；Pytest 153 项通过（另有一条来自依赖的弃用警告）；
- Vitest 10 项通过；TypeScript 检查与 Vite 生产构建通过；
- `tri-arb doctor` 的配置、SQLite、Protobuf、MEXC 四项公共 REST 与 OKX 三项公共 REST 全部通过；
- doctor 实时观测 MEXC 2,039 个规范化市场/0 个拒绝，OKX 1,308 个规范化市场/0 个元数据拒绝；
  OKX ticker 中 1 个不满足安全报价结构的市场被逐市场隔离；
- OKX 60 个候选市场的动态价格限制使用 10 路限流并发收齐；深度两分片均保持第 1 连接代次，订阅
  轮换后的迟到旧市场数据被安全丢弃，未触发重连；
- 最新内置浏览器运行环境固定为 1,280 px，完成桌面双交易所回归且无控制台错误；390 px 验收沿用
  v0.1 真实移动检查，并由新增响应式规则审查、组件测试和生产构建覆盖，未伪称完成新的移动实机检查。

Binance 公共 REST 适配增量验收：

- 真实公共生产响应归一化 1,366 个可交易现货市场、1,366 组 book ticker/24 小时活跃度，元数据与
  ticker 均为 0 个拒绝，时钟往返 307 ms；
- `exchangeInfo` 与 `executionRules` 并行读取，所有市场均取得公开 `PRICE_RANGE`；适配器未使用
  API Key，也未调用任何账户或交易端点；
- 5 项 Binance 专用离线契约测试覆盖规则归一化、手续费下限、逐市场隔离、公共端点集合与时钟校准。
- 真实 BTCUSDT `@depth@100ms` 流与 1,000 档 REST 快照按官方序列算法衔接成功，输出版本
  `97706342685` 的 20 bids + 20 asks，并保留交易所事件时间；无时间戳的 `@depth20` 未被采用。
- 动态 WebSocket 分片真实接收连续版本 `97706417862`、`97706417906`、`97706417946`，三本均为
  20 bids + 20 asks，连接始终保持第 1 代并完成 `connecting -> connected -> stopped` 干净状态转换。
- 同一真实连接同时收到 9 本 BTCUSDT 深度与参考价 `66469.46707631`；参考价携带 Binance 来源时间
  `1784655744007` 和第 1 连接代次，确认器已覆盖“本地新鲜但来源时间过期”的拒绝测试。
